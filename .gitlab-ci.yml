stages:
  - deploy

pages:
  stage: deploy
  image: alpine:3.19
  script:
    - mkdir -p public
    - cp -r softupakaran-clickable-slider-8085/* public/
  artifacts:
    paths:
      - public
  only:
    - main
